export type Box = {
  name: string
  readonly picture: string
  readonly version: string
  readonly icon?: string
}

export type APIKey = {
  readonly id: string
  readonly name: string
  readonly key: string
}
