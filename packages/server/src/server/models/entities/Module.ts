export type ModuleEntity = {
  id: string
  name: string
  // nickname?: string
  description: string
  version: string
  author: string
  configuration: object
  enabled: boolean
  // importedAt: Date
}
